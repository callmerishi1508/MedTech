import 'package:flutter/material.dart';

class HospitalAuthoritiesPage extends StatelessWidget {
  const HospitalAuthoritiesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hospital Authorities Page'),
      ),
      body: Center(
        child: Text('This is the Hospital Authorities Page'),
      ),
    );
  }
}
